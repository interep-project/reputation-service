import { SystemStyleObject } from "@chakra-ui/react"

const Modal = {
    baseStyle: (): SystemStyleObject => ({
        dialog: {
            bg: "background.800",
            borderRadius: "4px"
        }
    })
}

export default Modal
