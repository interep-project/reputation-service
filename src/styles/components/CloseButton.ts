const CloseButton = {
    baseStyle: {
        _focus: {
            boxShadow: "none"
        },
        borderRadius: "xl"
    },
    defaultProps: {
        size: "lg"
    }
}

export default CloseButton
