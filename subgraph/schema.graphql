type Group @entity {
    id: ID!
    groupId: Bytes
    name: String!
    members: [Member!]! @derivedFrom(field: "group")
}

type Member @entity {
    id: ID!
    identityCommitment: Bytes
    rootHash: Bytes
    leafIndex: Int!
    group: Group
}